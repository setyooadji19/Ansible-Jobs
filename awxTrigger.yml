---
- name: Hello World Sample
  hosts: all
  become: true
  tasks:
  - name: Copy readme file
    copy:
      src: README.md
      dest: /etc/README.md
    when: github-webhook.ref == "refs/heads/main"

  - name: Run apt update via shell
    shell: apt update
    become: yes
    
    #   tasks:
#    - name: Hello Message
#      debug:
#        msg: "Hello World!"

