---
- name: Hello World Sample
  hosts: all
  vars:
    ansible_connection: ssh
    ansible_user: isrdds
    ansible_ssh_password: labisr2020
  become: true
  tasks:
   - name: Hello Message
     debug:
       msg: "Hello World!"
#   tasks:
#   - name: Copy txt file
#     copy:
#       src: /etc/ansible/text.txt
#       dest: /etc/text2.txt

#   - name: Run apt update via shell
#     shell: apt update
#     become: yes

